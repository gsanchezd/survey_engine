<div class="se-container">
  <nav class="se-breadcrumb">
    <%= link_to "← Back to Surveys", surveys_path, class: "se-link" %>
  </nav>

  <div class="se-completion-page">
    <header class="se-completion-header">
      <h1 class="se-success-title">🎉 Survey Completed!</h1>
    </header>
    
    <div class="se-completion-card">
      <h2><%= @survey.title %></h2>
      <p class="se-thank-you">Thank you for completing the survey!</p>
      
      <% if @response %>
        <div class="se-response-summary">
          <h3>Your Response Summary</h3>
          <dl class="se-summary-list">
            <dt>Participant:</dt>
            <dd><%= @participant.email %></dd>
            
            <dt>Completed:</dt>
            <dd><%= @response.completed_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
            
            <dt>Time taken:</dt>
            <dd><%= time_duration_in_words(@response.completion_time.to_i) %></dd>
            
            <dt>Questions answered:</dt>
            <dd><%= @response.answers.count %> of <%= @survey.questions.count %></dd>
            
            <dt>Completion rate:</dt>
            <dd><%= @response.completion_percentage %>%</dd>
          </dl>
        </div>
      <% end %>
    </div>
    
    <% if @response&.answers&.any? %>
      <section class="se-answers-review">
        <h3>Your Answers</h3>
        
        <% @survey.questions.ordered.each_with_index do |question, index| %>
          <% answer = @response.answer_for_question(question) %>
          <article class="se-answer-card">
            <h4 class="se-question-title">
              <%= index + 1 %>. <%= question.title %>
            </h4>
            
            <div class="se-answer-display">
              <% if answer %>
                <strong>Your answer:</strong> <%= answer.complete_answer %>
              <% else %>
                <em class="se-text-muted">Not answered</em>
              <% end %>
            </div>
          </article>
        <% end %>
      </section>
    <% end %>
    
    <footer class="se-completion-footer">
      <%= link_to "Take Another Survey", surveys_path, 
          class: "se-btn se-btn-primary" %>
    </footer>
  </div>
</div>